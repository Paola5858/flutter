import 'package:flutter/material.dart';

/// Paleta de cores centralizada para o app AgroControl
/// Cores temáticas agricultureis para identidade visual forte
class AppColors {
  // Construtor privado para evitar instanciação
  AppColors._();

  // Cores de fundo
  static const fundoClaro = Color(0xFFF2EDE4); // bege-areia
  static const fundoEscuro = Color(0xFF0D1410); // verde-preto

  // Cor primária
  static const verdeOliva = Color(0xFF2D5016);

  // Cor de acento
  static const douradoTrigo = Color(0xFFD4A574);

  // Cor de texto claro
  static const cremeSol = Color(0xFFF8E8C4);

  // Cores de dados
  static const azulUmidade = Color(0xFF4A90D9);

  // Cores de alerta
  static const alertaSeco = Color(0xFFC0392B);
  static const alertaIdeal = Color(0xFF27AE60);
}
